SEMINARIO DE PROGRAMACIÓN EN PARALELO
- Muñiz Morales Erick. 

Cada archivo tiene el algoritmo en una función respectiva que depende de n, el número de nodos. 
En cada uno, se presentan una 'dataframe'. 
n (cantidad de nodos) , time (tiempo de ejecución). 


Nota:
Para correr el archivo 'secuencial.c', es igual a compilar cualquier programa em C

	gcc secuencial.c -o secuencial
	./secuencial

Para correr el código 'parallel.c'

	gcc -fopenmp parallel.c -o parallel
	./parallel.